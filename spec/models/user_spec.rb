require 'rails_helper'

RSpec.describe User, type: :model do
  # it "validates a user has a first name" do
  #     # user = User.create
  #     expect(users.errors).to be_empty
  # end
  # it "validates a user has a last name" do
  #     # user = User.create
  #     expect(users.errors).to be_empty
  # end
  # it "validates a user has a email" do
  #     # user = User.create
  #     expect(users.errors).to be_empty
  # end
  # it "validates a user has a password" do
  #     # user = User.create
  #     expect(users.errors).to be_empty
  # end
end
